import 'package:flutter/material.dart';

class TodaySessions extends StatelessWidget {
  const TodaySessions({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(child: Row());
  }
}
